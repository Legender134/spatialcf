"""Canonical v2 contracts for the platform-neutral SpatialCF core.

Consumers import concrete types from their defining modules.  This package
initializer deliberately re-exports nothing so importing it cannot pull the
evidence/publication layer into the semantic dependency graph.
"""
